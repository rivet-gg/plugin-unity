datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Token {
  id        String    @id @default(uuid()) @db.Uuid
  token     String    @unique
  type      String
  meta      Json
  trace     Json
  createdAt DateTime  @default(now())
  expireAt  DateTime?
  revokedAt DateTime?
}
// Generated by Open Game Backend
generator client {
	provider = "prisma-client-js"
	output = "/Users/nathan/rivet/plugin-unity/.opengb/prisma_workspace/db/e7820014-7a6d-4e3f-9d6c-d77c81bae935/client"
	previewFeatures = ["driverAdapters"]
}